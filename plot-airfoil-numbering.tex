\begin{tikzpicture}
  \begin{axis}[
    ybar,
    width=12cm,
    % height=3.5cm,
    enlarge x limits=0.25,
    xlabel = Renumbering applied,
	ylabel = Core-computation runtime (s),
    symbolic x coords={Original,Metis space-filling curve,Random},
    xtick=data,
    % nodes near coords, nodes near coords align={horizontal},
	legend pos = north west,
    ]

	\addplot [
	pattern=crosshatch,
	pattern color=red,
	] table [
	symbolic x coords =category,
	y = baseline
	] {data-airfoil-numbering.tsv};
	\addlegendentry{Baseline OP2};


	\addplot [
	pattern=horizontal lines light blue,
	] table [
	symbolic x coords = category,
	y = mine-unstr
	] {data-airfoil-numbering.tsv};
	\addlegendentry{Crystal/ no structure info};


	\addplot [
	pattern=north east lines,
	pattern color =green,
	] table [
	symbolic x coords = category,
	y = mine
	] {data-airfoil-numbering.tsv};
	\addlegendentry{Crystal/ structure info};

  \end{axis}
\end{tikzpicture}